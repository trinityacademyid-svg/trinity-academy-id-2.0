NEXT_PUBLIC_SUPABASE_URL=

# Preferred for newer Supabase projects. Used first when both keys are present.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Backward-compatible fallback for projects still using anon keys.
NEXT_PUBLIC_SUPABASE_ANON_KEY=
